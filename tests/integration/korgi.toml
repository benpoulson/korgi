[project]
name = "testapp"

# Integration test: host-a is both LB and node (co-located mode)
[[hosts]]
name = "host-a"
role = "lb"
address = "127.0.0.1"
port = 2201
user = "root"
ssh_key = "tests/integration/test_key"
labels = ["web"]

[[hosts]]
name = "host-b"
address = "127.0.0.1"
port = 2202
user = "root"
ssh_key = "tests/integration/test_key"
labels = ["web"]

[traefik]
image = "traefik:v3.2"
entrypoints = { web = ":80" }
network = "korgi-traefik"

[[services]]
name = "web"
image = "nginx:alpine"
replicas = 3
placement_labels = ["web"]

[services.health]
path = "/"
interval = "2s"
timeout = "2s"
retries = 3
start_period = "5s"

[services.routing]
rule = "Host(`test.local`)"
entrypoints = ["web"]

[services.ports]
container = 80

[services.deploy]
drain_seconds = 30
start_delay = 5
rollback_keep = 1
